// components/SendRequestButton.js
import React from 'react';

function SendRequestButton({ onClick }) {
  return <button onClick={onClick}>Send Request</button>;
}

export default SendRequestButton;
